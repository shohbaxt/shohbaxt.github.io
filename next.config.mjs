/** @type {import('next').NextConfig} */
const nextConfig = {
  output: "export",
  basePath: "/shohbaxt.github.io",
  images: { unoptimized: true },
  poweredByHeader: false,
};

export default nextConfig;
